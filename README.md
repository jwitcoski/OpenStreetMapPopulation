# BuildingPop (published site)

This branch serves the live GitHub Pages site.

**Live:** https://jwitcoski.github.io/OpenStreetMapPopulation/

Source code lives on `cursor/modernize-overpass-gh-pages-87ef` (and future feature branches). Rebuild with `npm run build` and publish the `dist/` folder here.

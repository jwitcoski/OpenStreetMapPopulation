{
  "name": "openstreetmap-population",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:draw": "node tests/draw-smoke.mjs",
    "test:mobile": "node tests/mobile-draw-overpass.mjs",
    "test:unit": "node tests/heatmap-unit.mjs && node tests/overpass-failover.mjs",
    "test": "npm run test:unit && npm run test:draw && npm run build && npm run test:mobile"
  },
  "dependencies": {
    "@turf/area": "^7.2.0",
    "@turf/helpers": "^7.2.0",
    "maplibre-gl": "^5.6.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "playwright": "^1.62.0",
    "vite": "^7.0.0"
  }
}
